namespace DataAccess.Enums
{
    public enum AttendanceStatus
    {
        REGISTERED,
        PENDING,
        WAITLIST,
        CANCELLED,
        PRESENT,
        CHECKED_IN,
        ABSENT,
        REJECTED
    }
}
