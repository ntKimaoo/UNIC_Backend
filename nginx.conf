events {
    worker_connections 1024;
}

http {
    # ── Rate limiting ──
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;

    upstream app_servers {
        server unic-backend:8080;
    }

    # ── HTTP → redirect to HTTPS ──
    server {
        listen 80;
        server_name chuyencongnhan.io.vn;

        # Let's Encrypt challenge
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # ── HTTPS ──
    server {
        listen 443 ssl;
        server_name chuyencongnhan.io.vn;

        # SSL certificates (Let's Encrypt)
        ssl_certificate     /etc/letsencrypt/live/chuyencongnhan.io.vn/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/chuyencongnhan.io.vn/privkey.pem;

        # SSL settings
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;

        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;

        # ── API proxy ──
        location / {
            proxy_pass http://app_servers;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket support (for SignalR)
            proxy_read_timeout 86400s;
            proxy_send_timeout 86400s;
        }
    }
}